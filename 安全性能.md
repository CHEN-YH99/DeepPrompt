# Deeprompt 安全性能审计与规划

> 审计时间：2026-05-14
> 审计范围：`apps/web`、`apps/api`、`packages/*`
> 审计方式：静态代码分析（仅读，未运行也未修改任何文件）

## 摘要

- **安全**：5 项 Critical + 9 项 High。公网上线前必须把 P0 全部清零，否则 cookie 明文劫持、JWT 伪造、`.env` 历史泄漏任意一条都可能让站点一夜归零。
- **性能**：6 项 High Impact。最大单点瓶颈是 `pg.Client` 单连接（API 全程 DB 并发为 1），其次是 Shell 在每页重复挂载、列表无缓存、图片未走 `next/image`。
- **优秀做法**：SQL 全参数化、refresh token 用 SHA-256 哈希入库、bcrypt cost 12、CORS 走 origin 白名单、滑块验证码 UI、SSR + revalidate 已经使用。基础底子尚可，但安全和性能各有"半成品"。

完整修复路线图见文末。

---

## 第一部分：安全审计

下方按维度展开，每一项格式：`严重等级 — 位置 — 问题 — 修复`。

### 认证与会话

- **Critical** — [apps/api/src/index.ts:168-170](apps/api/src/index.ts#L168) JWT 兜底密钥硬编码
  - 问题：`JWT_SECRET ?? "replace_me_with_real_jwt_secret"` 和 `JWT_REFRESH_SECRET ?? "replace_me_with_real_jwt_refresh_secret"`。生产环境若忘配环境变量，服务静默使用公开弱密钥 → 攻击者可任意伪造 `role: admin` token。
  - 修复：删除 fallback，启动时检查 `process.env.JWT_SECRET` 不存在或长度 < 32 直接 `throw`；用 `crypto.randomBytes(64).toString('hex')` 生成。

- **Critical** — [login/route.ts:50,54,59,63,68,72](apps/web/app/api/auth/login/route.ts#L50) cookie 全部 `secure: false`
  - 问题：access_token / refresh_token / user_nickname 三个 cookie 都强制非安全，HTTPS 部署也会以明文传输 → MITM 直接劫持。
  - 修复：`secure: process.env.NODE_ENV === "production"` 或读 `COOKIE_SECURE` 环境变量。

- **Critical** — [session/route.ts:56,84](apps/web/app/api/auth/session/route.ts#L56) 同 `secure: false`
- **Critical** — [middleware.ts:51](apps/web/middleware.ts#L51) middleware 静默刷新时仍 `secure: false`
- **High** — [apps/api/src/index.ts:771](apps/api/src/index.ts#L771) 后端 refresh_token cookie 也 `secure: false`，且前端已写一遍，存在双写。
- **High** — [login/route.ts:67](apps/web/app/api/auth/login/route.ts#L67) `user_nickname` cookie `httpOnly: false`
  - 问题：明文昵称 cookie 可被任意 JS 读取，是不必要的暴露面。
  - 修复：删该 cookie，SSR 改通过受信任的 `/api/auth/session` 拉昵称；或限定为 ASCII 字符集并明确仅作 UI hint。
- **High** — [apps/api/src/index.ts:787-843](apps/api/src/index.ts#L787) `/v1/auth/refresh` 不轮换 refresh_token
  - 修复：实现 rotation：每次刷新签发新 refresh，旧 session 标记 `revoked`，检测到旧 refresh 被重放时主动撤销该用户所有 session。
- **Medium** — [apps/api/src/index.ts:413-432](apps/api/src/index.ts#L413) `requireAuth` 不区分过期 vs 非法 token，前端无法触发 silent refresh。
- **Medium** — refresh token 用裸 SHA-256 入库（[index.ts:332](apps/api/src/index.ts#L332)），无 server-side pepper。建议切 HMAC-SHA256 + 服务端密钥。

### 密码

- **Medium** — [apps/api/src/index.ts:622](apps/api/src/index.ts#L622) 后端密码强度仅校验长度 ≥ 8
  - 修复：加复杂度（含字母+数字）、长度 ≥ 10、对照本地常见密码黑名单。
- **Medium** — [apps/api/src/index.ts:675](apps/api/src/index.ts#L675) 用 `bcryptjs`（纯 JS，慢 3-4 倍）
  - 修复：迁 `argon2`（OWASP 当前推荐）或至少切 native `bcrypt`。
- **Low** — 前后端密码强度规则不一致：[register-form.tsx:138](apps/web/components/register-form.tsx#L138) 前端有强度提示但后端没对齐。
- **Low** — `NEXT_PUBLIC_API_BASE_URL` 实际只在 server 用，建议改名 `API_BASE_URL` 避免误进入客户端 bundle。

### SQL 注入

- **Low / 整体良好** — 49 处 `pgClient.query` 全部参数化 ✓。`SORT_FIELDS` 白名单映射 `ORDER BY` 安全。
- **Medium** — [apps/api/src/index.ts:1447,1464](apps/api/src/index.ts#L1447) `applyInteraction` 用 `${countColumn}` 拼列名，目前来自二选一字面量安全，但建议改成显式白名单常量，避免重构误伤。

### XSS

- **Low** — [prompts/[id]/page.tsx:126](apps/web/app/prompts/[id]/page.tsx#L126) `dangerouslySetInnerHTML` 注入 ld+json
  - 问题：用户控制的 title/excerpt/prompt 拼进 `<script type="application/ld+json">`，攻击者写 `</script><script>...` 可逃逸。
  - 修复：`JSON.stringify(data).replace(/</g, "\\u003c")`（Next.js 官方推荐做法）。
- **Low** — [prompt-card.tsx:80](apps/web/components/prompt-card.tsx#L80) / [prompt-gallery.tsx:32,50](apps/web/components/prompt-gallery.tsx#L32) `<img src={userUrl}>` 未过滤 `javascript:` 协议。
  - 修复：URL 协议白名单（http/https/相对路径）。

### CSRF

- **High** — login/register/logout/publish/me/interactions/admin/moderation 全部 POST 接口零 CSRF 防护
  - 问题：cookie 是 `sameSite: "lax"`，可阻挡跨站异步 POST，但顶级导航触发的 `<form method="post">` 仍会带 cookie。
  - 修复：所有状态改变接口校验 `Origin` 或 `Sec-Fetch-Site === "same-origin"`；或上 double-submit token。
- **Medium** — [apps/api/src/index.ts:190-198](apps/api/src/index.ts#L190) CORS `WEB_ORIGIN` 缺省到 `http://localhost:3000`，生产忘配会回退到危险默认。建议启动失败而非降级。

### 授权 / 越权

- **High** — [apps/api/src/index.ts:1121-1178](apps/api/src/index.ts#L1121) `/v1/prompts/me` admin 视角默认拉全量
  - 问题：admin/moderator 调用此接口返回所有用户所有状态 prompts，一旦 admin 账号被入侵 = 全库被拉走。
  - 修复：拆独立 `/v1/admin/prompts` 接口 + audit 日志 + 限流。
- **Medium** — [apps/api/src/index.ts:434-447](apps/api/src/index.ts#L434) `requireRole` 实现 hack（Promise 包装 + 判 `if (!user) return`），易在重构中漏掉 `next` 链路阻断。
- **Low** — like/collect DELETE 不校验 prompt 是否存在，可被用来探测 promptId 是否存在（轻微信息泄漏）。

### 输入校验

- **Medium** — `/v1/prompts/me LIMIT 100`、`/v1/admin/moderation LIMIT 50`、`/v1/me/collections LIMIT 100`、`/v1/invites LIMIT 100` 都硬编码上限，**没有 offset/cursor 分页**，数据量大后无法翻页且 OOM。
- **Medium** — [apps/api/src/index.ts:1297-1307](apps/api/src/index.ts#L1297) 创建 prompt 的 `images.url` 入库无校验，可塞 `javascript:` 或外部恶意 URL。
- **Medium** — [apps/api/src/index.ts:1700](apps/api/src/index.ts#L1700) telemetry payload 直接 `JSON.stringify` 入库，无显式 size 限制。
- **Low** — 上传扩展名白名单依赖 `path.extname(filename)`，注意 `image.png%00.html` 空字节技巧。

### 限流与暴力破解

- **High** — [apps/api/src/index.ts:222-248](apps/api/src/index.ts#L222) **`rateLimit` 在无 Redis 时整体禁用**
  - 问题：`if (!redisClient) { next(); return; }` 后面用的是内存 Map 而不是 redis，判断与实现脱节 → 两边都没生效。当前等于完全无限流。
  - 修复：去掉 `if (!redisClient)` 这行，让内存桶兜底；或者真用 redis 实现。
- **High** — login/register 限流 20/min/IP 太宽松，且未按 account 计数 → 离线词典攻击成本极低。
  - 修复：账户级失败 5 次锁定 15 分钟。
- **Critical** — [slider-captcha.tsx](apps/web/components/slider-captcha.tsx) 滑块验证码**纯客户端**
  - 问题：验证逻辑全在浏览器，后端 login 接口完全不知道验证状态。攻击者直接 POST `/v1/auth/login` 即可跳过。基本等于装饰品。
  - 修复：接 hCaptcha / Cloudflare Turnstile 等服务端可验证方案。

### 敏感信息泄漏

- **Critical** — **`.env` 文件被 git 追踪**
  - 问题：`.gitignore` 没列 `.env`，文件已 tracked。明文写入 `DATABASE_URL`、JWT 密钥占位。一旦推 GitHub，commit history 永久泄漏。
  - 修复：1) `git rm --cached .env` + 加 `.gitignore`；2) rewrite history 或视为密钥已泄漏立即轮换（DB 密码、JWT_SECRET、JWT_REFRESH_SECRET、R2_*）；3) 保留 `.env.example`。
- **High** — [apps/api/src/index.ts:1953](apps/api/src/index.ts#L1953) 全局错误处理器把 `error.message` 回传客户端
  - 问题：DB 错误、堆栈摘要、连接字符串错误等可能裸奔到响应。多处也直接把 `error.message` 当 message 回前端。
  - 修复：生产模式只返回固定 `"Internal server error"`，错误细节写结构化日志（含 reqId）。
- **High** — [apps/web/app/api/prompts/route.ts:70-216](apps/web/app/api/prompts/route.ts#L70) 大量 `console.log` 含敏感数据（rawBody slice、payloadSummary、token cookie 状态）。生产容器日志持续暴露用户提交内容。
- **Medium** — [apps/api/src/index.ts:266-281](apps/api/src/index.ts#L266) 请求日志记 `ip` 和 `userAgent`，未脱敏 query，按 GDPR 应有保留期与脱敏策略。

### 依赖

- **High** — [apps/api/package.json:25](apps/api/package.json#L25) `multer: ^1.4.5-lts.2`
  - multer 1.x 已 EOL，多个已披露 CVE（CVE-2024-4068 ReDoS、CVE-2025-7338 path）。可能是死代码（项目里没看到 import），先确认再决定升 2.x 或删依赖。
- **High** — `bcryptjs ^2.4.3` 见密码维度。
- **Medium** — `express ^4.21.2` 仍是 4.x，5.x 已 GA 修复了 path-to-regexp 多个 ReDoS。
- **Medium** — `next ^16.2.4` 配 `react ^18.3.1`，Next 16 推荐 React 19，版本错配可能触发 peer warning 或 hydration 边界 bug。

### CORS / HTTPS / 文件路径

- **Medium** — CORS 未限制 `methods` / `allowedHeaders`，默认放行所有。建议显式 `methods: ["GET","POST","PUT","DELETE","PATCH"]`。
- **Medium** — 所有 cookie 硬编码 `secure: false`，详见认证维度。
- **Medium** — `image_url` 任意 URL 入库（无后端抓取 = 无 SSRF，但可作 phishing/tracking）。建议仅允许 R2 公开域名或本站 `/uploads/`。
- **Medium** — `apps/web/app/api/prompts/route.ts:42-67` `persistUploadedFile` 已检测 `targetPath.startsWith(uploadRoot)` ✓ 防穿越，正确。

### 安全修复优先级

| # | 严重等级 | 位置 | 一句话 |
|---|---|---|---|
| 1 | Critical | `.env` 被 git 追踪 | 立刻 `git rm --cached` + 轮换所有密钥 |
| 2 | Critical | `apps/api/src/index.ts:168-170` | 删除 JWT 兜底密钥，启动校验 |
| 3 | Critical | login/session/logout/middleware 全部 cookie `secure: false` | 切环境判断 |
| 4 | Critical | `slider-captcha.tsx` | 客户端验证码可绕过，接服务端可验证方案 |
| 5 | High | `apps/api/src/index.ts:222-248` rateLimit 整体禁用 | 修复逻辑或真接 Redis |
| 6 | High | `apps/api/src/index.ts:787-843` refresh 不轮换 | 实现 rotation + reuse detection |
| 7 | High | 所有 POST 接口无 CSRF 防护 | Origin 校验 / CSRF token |
| 8 | High | `apps/api/src/index.ts:1953` 错误堆栈裸奔 | 生产仅输出通用文案 |
| 9 | High | `apps/web/app/api/prompts/route.ts` 调试 console.log | 删除或加 log level |
| 10 | High | `multer 1.x` + `bcryptjs` 老旧依赖 | 升 multer 2 / 切 argon2 |
| 11 | High | admin 视角无审计 | 拆接口 + audit log |
| 12 | High | login 速率 20/min/IP 太宽松 | 改账户级失败锁定 |
| 13 | High | `user_nickname` cookie `httpOnly: false` | 删除或限制内容 |
| 14 | Medium | 密码强度仅长度校验 | 加复杂度 + 黑名单 |
| 15 | Medium | `applyInteraction` 列名插值 | 显式白名单常量 |
| 16 | Medium | ld+json 未转义 `</script>` | `replace(/</g,"\\u003c")` |
| 17 | Medium | `<img src>` 未过滤 `javascript:` 协议 | URL 协议白名单 |
| 18 | Medium | CORS 缺 methods/headers 显式 | 收紧白名单 |
| 19 | Medium | 列表分页全部硬限 | 引入 cursor 分页 |
| 20 | Medium | `image_url` 任意 URL 入库 | 域名白名单 |
| 21 | Low | 文件名空字节风险 | `\0` 过滤 |
| 22 | Low | refresh hash 用 SHA-256 而非 HMAC | 加 server pepper |
| 23 | Low | DELETE 接口无 prompt 存在性检查 | 探测面收敛 |
| 24 | Low | 日志含 PII | 保留期策略 |

---

## 第二部分：性能审计

### 路由架构

- **High Impact** — [shell.tsx:12-67](apps/web/components/shell.tsx#L12) Shell 在每个 page.tsx 单独 import，路由切换时整树重渲染（含 cookies() + UserNav）
  - 修复：提升为根/路由组 `layout.tsx`，UserNav 用 `<Suspense>` 包成 client island，路由切换只重渲染 children。
- **Medium** — [layout.tsx:62-73](apps/web/app/layout.tsx#L62) 一次性挂 4 个 client provider（Telemetry/CacheSync/ScrollState/Typewriter）全部全局 `useEffect`
  - 修复：合并为单个 `ClientProviders`；`TypewriterHeadline` 用 `next/dynamic({ ssr:false })` 延迟到 idle 时执行。
- **Medium** — [typewriter-headline.tsx:69-72](apps/web/components/typewriter-headline.tsx#L69) `MutationObserver(document.body, subtree:true)` 是 Long Task 源头
  - 修复：换 `IntersectionObserver` 或 `useLayoutEffect` 跑一次即可。
- **Low** — [me/prompts/page.tsx](apps/web/app/me/prompts/page.tsx) 缺 `export const dynamic = "force-dynamic"`，依赖 cookies+no-store 自动判定，建议显式标注避免误改导致 ISR 缓存私有数据。
- **Low** — `/prompts/[id]` 详情页对匿名用户应允许 SSG/ISR，只在 cookie 存在时走动态路径。
- **观察** — components 22 个 .tsx 中 22 个 `"use client"`，比例偏高。`prompt-card` 这类纯展示组件被强制 client，每个 prompt 都成为 hydration 节点。

### 数据库查询

- **🔥 Highest Impact** — [apps/api/src/index.ts:179](apps/api/src/index.ts#L179) **用单 `pg.Client` 而非 `pg.Pool`**
  - 问题：整 API 进程串行排队同一连接，**DB 并发 = 1**，所有 `Promise.all` 实际是串行执行。这是当前最大单点瓶颈。
  - 修复：改 `new Pool({ max: 10 })`，所有 `pgClient.query` 改 `pool.query`，事务用 `pool.connect()`。
- **High** — [apps/api/src/index.ts:1019-1050](apps/api/src/index.ts#L1019) `/v1/prompts` 列表 cover_url 是行级子查询（列表 N 次 + 封面 N 次）
  - 修复：用 `LEFT JOIN LATERAL` + `prompt_images` 上覆盖索引；或物化 `prompts.cover_url` 列发布时同步。
- **High** — [apps/api/src/index.ts:1052-1102](apps/api/src/index.ts#L1052) 4 个 facet 查询 + list + total = 6 条 SQL/列表
  - 修复：facet 用单条 `GROUPING SETS`/`UNION ALL` 一次往返；列表加 30s Redis 缓存（key 含查询哈希）。
- **Medium** — [apps/api/src/index.ts:1104](apps/api/src/index.ts#L1104) `toPromptListItem` 用 `getModelLabel` 覆盖了 SQL 已返回的 `model_label` 字段（冗余计算）。
- **Medium** — [apps/api/src/index.ts:1180-1213](apps/api/src/index.ts#L1180) `/v1/prompts/:id` 最多 4 次 DB 往返（getPromptDetail + user + owner 二次 + interactions）
  - 修复：owner check 合并到 getPromptDetail；interactions 用 `EXISTS` 子查询 join 一起拿。
- **Medium** — [apps/api/src/index.ts:1216-1272](apps/api/src/index.ts#L1216) related 用 `cardinality(ARRAY(SELECT UNNEST... INTERSECT...))` 全表扫描
  - 修复：先用 `model_ids && $2::text[]` 命中 GIN 索引拿候选集，再 SQL 内排序。
- **Medium** — [apps/api/src/index.ts:1364-1380](apps/api/src/index.ts#L1364) 创建 prompt 时 images 循环串行 INSERT
  - 修复：单条 multi-row VALUES 或 `pg-format` 批量插入。
- **索引缺口** — [packages/database/src/schema.sql](packages/database/src/schema.sql)：
  - `prompts.copy_count DESC` / `like_count DESC` / `collect_count DESC` 缺单列部分索引：`CREATE INDEX ... WHERE status='approved'`。
  - `usage_tags` / `color_tags` 缺 GIN 索引，但 search 路径会用 `&&` 过滤。

### 缓存

- **High Impact** — `/v1/prompts` 列表（无 token）未走 Redis 缓存
  - 修复：30-60s Redis 缓存（key 含查询哈希），预计减 70%+ DB 压力。
- **Medium** — [apps/api/src/index.ts:79-129](apps/api/src/index.ts#L79) modelCache 是进程内 60s，多实例不一致
  - 修复：迁 Redis + pub/sub 失效。
- **Medium** — `fetchCurrentUser` 在 home/detail/me/admin 每个 SSR page 都被调用，未跨页复用
  - 修复：用 `React.cache`/`unstable_cache` 请求作用域 dedupe。
- **Medium** — [me/interactions/route.ts:43-46](apps/web/app/api/me/interactions/route.ts#L43) 与 [api/prompts/route.ts:226-235](apps/web/app/api/prompts/route.ts#L226) 一次操作 revalidateTag 4 个标签 + 多个 path → 并发互动场景下放大失效风暴
  - 修复：仅 invalidate 详情 path/tag，列表用 BroadcastChannel 触发当前 tab `router.refresh`（已有 cache-sync 机制）。

### 图片与静态资源

- **High Impact** — [prompt-card.tsx:78-90](apps/web/components/prompt-card.tsx#L78) / [prompt-gallery.tsx:30-54](apps/web/components/prompt-gallery.tsx#L30) 全部使用原生 `<img>`，未走 `next/image`
  - 修复：换 `next/image` + `sizes="(max-width:720px) 50vw, 320px"` + `priority={index<2}`，LCP 优化。
- **Medium** — `apps/web/next.config.mjs` 已配 `remotePatterns` 但未启用 `formats`/`deviceSizes`/`minimumCacheTTL`
  - 修复：`formats: ['image/avif','image/webp']`, `minimumCacheTTL: 30天`, `dangerouslyAllowSVG: false`。
- **Medium** — [apps/web/lib/data.ts:151-263](apps/web/lib/data.ts#L151) fallback prompt cover 全挂 `images.unsplash.com`，被限流时 LCP 爆炸
  - 修复：fallback 图迁 R2 公共桶。

### 客户端 bundle

- **Medium** — [publish-loading-overlay.tsx](apps/web/components/publish-loading-overlay.tsx) 188 行复杂动画 + 多 timer，被 publish/page.tsx 静态 import
  - 修复：`next/dynamic(() => import('...'), { ssr:false })` 在用户点 submit 时才加载。
- **Medium** — [slider-captcha.tsx](apps/web/components/slider-captcha.tsx) 仅 login/register 用，但走静态 import，同 dynamic。
- **Medium** — [dictionaries.ts](apps/web/lib/i18n/dictionaries.ts) 43.5KB / 1248 行，被 client 组件如 [prompt-card.tsx:30](apps/web/components/prompt-card.tsx#L30) `import` 整模块（含全部 locale）
  - 修复：拆 `server-only` 文件 + client 仅接收 props；或按 namespace 拆分按需 import。

### React 渲染

- **Medium** — [prompt-card.tsx:37-65](apps/web/components/prompt-card.tsx#L37) 每张卡片维护 4 个 useState，列表 24 张 = 96 个 state，mousemove 触发整张卡片重渲染
  - 修复：mousemove 直接写 CSS 变量 `el.style.setProperty('--tx', x)`，避免 React state；lightbox 提升到列表级单例。
- **Medium** — [me/prompts/page.tsx:38-40](apps/web/app/me/prompts/page.tsx#L38) 分页用 `allRecords.slice()`，但 API 已是 LIMIT 100 全量
  - 修复：page/pageSize 透传 API，DB 层 LIMIT/OFFSET 或 keyset 分页。
- **Medium** — [models/page.tsx:60-84](apps/web/app/models/page.tsx#L60) 每个 model 做 `promptRecords.filter(...)` O(M*N)
  - 修复：SQL 端 `model_id, COUNT(*) GROUP BY` 一次返回。
- **Low** — 列表无 virtualization，目前规模 OK；`/me/collections` LIMIT 100 + PromptCard 重型组件，50+ 时引入 `react-virtuoso`。

### Hydration / 首屏

- **Medium** — [page.tsx:14-30](apps/web/app/page.tsx#L14) home `Promise.all([fetchModels, fetchPromptRecords])` 已并行，但 `fetchCurrentUser(accessToken)` 是串行第二阶段
  - 修复：合并进同一个 Promise.all。
- **Medium** — [prompts/[id]/page.tsx:70-82](apps/web/app/prompts/[id]/page.tsx#L70) 两段串行 Promise.all（先 prompt+user，再 related+models）
  - 修复：4 个 fetch 一次性并行（id 来自 params 立即可用）。
- **Medium** — `getDictionary()` 多次重复调用，建议 `React.cache` 包装。
- **Low** — 没有 `<Suspense>` 包裹 `{children}`，整页等 server 渲染完才返回。建议把次屏内容 stream。

### API 调用

- **High Impact** — [user-nav.tsx:50-90](apps/web/components/user-nav.tsx#L50) 每次 mount 都 fetch `/api/auth/session`，最坏链 3 次 API 往返
  - 修复：1) Shell SSR 已有 `initialNickname`，client 端**仅在 cookie 缺失时 fetch**；2) module-level Promise singleton 而非 useEffect。
- **Medium** — [copy-prompt-button.tsx:55-65](apps/web/components/copy-prompt-button.tsx#L55) 复制后 fetch `/api/prompts/:id/copy` 无 debounce，连点都打 DB UPDATE
  - 修复：1.5s 内只发一次。
- **Low** — [middleware.ts:22-53](apps/web/middleware.ts#L22) matcher 应进一步排除 `_next/data` 和 `/uploads`，限定到 page 路径。

### CSS / 动画

- **Medium** — [globals.css](apps/web/app/globals.css) 1929 行 / 36KB 单文件未拆分
  - 修复：拆 `globals.css`（变量+reset+排版）+ 路由局部 CSS Module（publish-loading.module.css、captcha.module.css）。
- **Medium** — 滚动条 `*::-webkit-scrollbar`（[globals.css:111-166](apps/web/app/globals.css#L111)）对所有元素生效，每个滚动元素都成合成层
  - 修复：限定到 `html, body, .scrollable` 等明确容器。
- **Low** — `will-change` 用在 section-reveal 和每个 typewriter-char span，动画结束未移除 → GPU 内存压力。
- **Low** — 多个 `repeating-linear-gradient` + `mix-blend-mode: screen` + 滚动条扫描动画并发，低端 GPU 易掉帧。

### 网络中间件

- **High Impact** — [apps/api/src/index.ts:166-200](apps/api/src/index.ts#L166) 未启用 `compression`
  - 修复：`app.use(compression({ threshold: 1024 }))`，prompts 列表 JSON 至少省 60-70%。
- **Medium** — 未 `app.set('trust proxy', 1)`，`req.ip` 不走 X-Forwarded-For → rate limit 拿不到真实 IP。
- **Medium** — 未挂 `helmet`，缺基本安全头。
- **Low** — rate limit Map 无清理机制，长跑泄漏；建议 setInterval 清理或换 `rate-limiter-flexible`。

### 性能优化优先级

| 优先级 | 项 | 文件 | 预估收益 |
|---|---|---|---|
| **High** | API 改用 `pg.Pool` | `apps/api/src/index.ts:179` | 解锁 DB 并发，p95 ↓50%+ |
| **High** | Shell 提升到 layout | `shell.tsx` + 12 page.tsx | TTFB ↓100-300ms |
| **High** | UserNav 仅 cookie 缺失时 fetch | `user-nav.tsx:50` | 每次切页省 1-3 个往返 |
| **High** | `/v1/prompts` 加 Redis 30s 缓存 + facet 合并 | `apps/api/src/index.ts:971-1119` | DB QPS ↓70% |
| **High** | PromptCard 换 `next/image` | `prompt-card.tsx`, `prompt-gallery.tsx` | LCP ↓1-2s |
| **High** | API 启用 compression | `apps/api/src/index.ts:190` | 响应体 ↓60-70% |
| Medium | 首页/详情 fetch 并行 | `page.tsx:14-30`, `prompts/[id]/page.tsx:70-82` | TTFB ↓100-200ms |
| Medium | 重型 client dynamic import | `publish-loading-overlay`, `slider-captcha`, `typewriter-headline` | First Load JS ↓20-40KB |
| Medium | revalidateTag 收敛 | `me/interactions/route.ts:43` | 减 ISR 雪崩 |
| Medium | 三个 count 列加部分索引 | `packages/database/src/schema.sql` | trending 查询 ↓ |
| Medium | dictionaries 拆 namespace 按需 import | `prompt-card.tsx:30` | First Load JS ↓ |
| Medium | TypewriterHeadline 去 subtree observer | `typewriter-headline.tsx:69` | 切页 Long Task ↓ |
| Low | cover_url 物化到 prompts 表 | `apps/api/src/index.ts:1036` | 减子查询 |
| Low | will-change 动画结束移除、滚动条限定容器 | `globals.css` | GPU 内存 |
| Low | globals.css 拆分 | `globals.css` 1929 行 | 非首屏 CSS ↓20KB |
| Low | `/v1/prompts/me` 改真分页 | `apps/api/src/index.ts:1121` | 数据规模后明显 |
| Low | bcrypt 12 → argon2 或 10 rounds | `apps/api/src/index.ts:675` | 登录/注册 CPU |
| Low | rate limit Map 清理 | `apps/api/src/index.ts:220` | 长跑内存泄漏 |
| Low | middleware matcher 排除静态资源 | `apps/web/middleware.ts:58` | RSC 链路开销 |

---

## 第三部分：修复路线图

按上线节奏切三段，先把破坏性的清零，再做体验和长期债务。

### P0 ── 公网上线前必须（预计 1-2 天）

| # | 任务 | 来源 |
|---|---|---|
| P0-1 | `.env` 从 git 历史移除，所有真实密钥轮换 | 安全 #1 |
| P0-2 | 删除 JWT 兜底密钥，启动校验密钥长度 ≥ 32 | 安全 #2 |
| P0-3 | 所有 cookie `secure` 按 `NODE_ENV` 切换 | 安全 #3 |
| P0-4 | 滑块验证码接服务端可验证方案（Turnstile / hCaptcha） | 安全 #4 |
| P0-5 | 修复 rateLimit 逻辑错位（去掉 `if (!redisClient) next()`） | 安全 #5 |
| P0-6 | API 改 `pg.Pool` | 性能 #1 |
| P0-7 | 全局错误处理器生产仅输出通用文案 | 安全 #8 |
| P0-8 | 删除 `apps/web/app/api/prompts/route.ts` 的调试 console.log | 安全 #9 |

### P1 ── 上线后一周内（预计 3-5 天）

| # | 任务 | 来源 |
|---|---|---|
| P1-1 | refresh token rotation + reuse detection | 安全 #6 |
| P1-2 | 所有 POST 接口加 Origin / CSRF token 校验 | 安全 #7 |
| P1-3 | login/register 改账户级失败锁定 | 安全 #12 |
| P1-4 | 升级 multer 2.x 或确认死代码后删除；bcryptjs → argon2 | 安全 #10 |
| P1-5 | admin 视角拆 `/v1/admin/prompts` + audit log | 安全 #11 |
| P1-6 | 删除 `user_nickname` cookie 或限制为 ASCII | 安全 #13 |
| P1-7 | Shell 提升到 root layout | 性能 #2 |
| P1-8 | UserNav 仅 cookie 缺失时 fetch | 性能 #3 |
| P1-9 | `/v1/prompts` 列表加 Redis 30s 缓存 + facet 合并 | 性能 #4 |
| P1-10 | PromptCard / PromptGallery 换 `next/image` | 性能 #5 |
| P1-11 | API 启用 `compression` + `trust proxy` + `helmet` | 性能 #6 |

### P2 ── 一个月内（持续偿还技术债）

| # | 任务 | 来源 |
|---|---|---|
| P2-1 | 列表分页统一改 cursor 分页 | 安全 #19 + 性能 #16 |
| P2-2 | 密码强度加复杂度 + 黑名单 | 安全 #14 |
| P2-3 | ld+json 转义 `</script>` | 安全 #16 |
| P2-4 | `<img src>` / `image_url` URL 协议白名单 | 安全 #17, #20 |
| P2-5 | CORS 收紧 methods/headers | 安全 #18 |
| P2-6 | 首页/详情 fetch 并行重排 | 性能 #7 |
| P2-7 | 重型 client 组件 dynamic import | 性能 #8 |
| P2-8 | revalidateTag 风暴收敛 | 性能 #9 |
| P2-9 | 加 count 列部分索引、cover_url 物化 | 性能 #10, #13 |
| P2-10 | dictionaries 按 namespace 拆分 | 性能 #11 |
| P2-11 | TypewriterHeadline 换 IntersectionObserver | 性能 #12 |
| P2-12 | globals.css 拆模块 + will-change 收敛 | 性能 #14, #15 |
| P2-13 | refresh token hash 换 HMAC + pepper | 安全 #22 |
| P2-14 | 错误码区分过期/非法 token | 安全 (Medium 杂项) |

---

## 总评

**底子 7/10**：SQL 全参数化、refresh 哈希入库、bcrypt cost、CORS 白名单这些"专业活"做得对；React 19/Next 16/Turbopack 这一栈也现代。

**致命弱点 3 处**：
1. 密钥/cookie 安全配置全员"开发模式"硬编码（`secure: false` 写死、JWT fallback、`.env` tracked），导致一旦公网部署即裸奔。
2. 客户端验证码 + rateLimit 逻辑错位 + 单 IP 限流过宽，组合起来给离线词典攻击开了高速通道。
3. `pg.Client` 单连接是性能黑洞，所有 `Promise.all` 实际串行，未来流量稍涨就 p99 飘红。

把 P0 八项搞定就能上线；P1 是体验和安全增益，做完才算"对得起用户"；P2 是技术债持续偿还，随业务迭代消化即可。
