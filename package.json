{
  "name": "deepprompt",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "predev:web": "node -e \"const [major,minor]=process.versions.node.split('.').map(Number); if(major<20||(major===20&&minor<9)){console.error('Node version must be >=20.9. Current: '+process.versions.node); process.exit(1)}\"",
    "prebuild": "node -e \"const [major,minor]=process.versions.node.split('.').map(Number); if(major<20||(major===20&&minor<9)){console.error('Node version must be >=20.9. Current: '+process.versions.node); process.exit(1)}\"",
    "dev": "turbo run dev --parallel",
    "dev:web": "npm run dev --workspace @deepprompt/web -- --port 3000",
    "dev:api": "npm run dev --workspace @deepprompt/api",
    "db:init": "npm run migrate --workspace @deepprompt/database",
    "build": "npm run build --workspaces --if-present",
    "start:web": "npm run start --workspace @deepprompt/web -- --port 3000",
    "start:api": "npm run start --workspace @deepprompt/api",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "turbo": "^2.5.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
