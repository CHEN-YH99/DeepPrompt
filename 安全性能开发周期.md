# Deeprompt 安全性能开发周期

## 1. 文档定位

这份文档不是"安全性能.md"的复制粘贴，而是把那份审计清单**按时间窗口拆成可执行的关卡**：每一关有目标、有任务清单、有交付物、有验收标准、有风险提醒。

读这份文档的姿势：

- 排期 / 立项：直接把每个关卡当成 sprint backlog
- 验收 / 复盘：照着"验收标准"逐条勾
- 风险评估：风险提醒一栏写的是这关最容易翻车的地方

依赖文档：

- [安全性能.md](安全性能.md) —— 所有发现项的原始清单与文件位置
- [Deeprompt_技术实现文档.md](DeepPrompt_技术实现文档.md)
- [Deeprompt_开发周期闯关.md](DeepPrompt_开发周期闯关.md)

---

## 2. 总周期建议

整个治理周期切成 6 关，**串行优先级强**：关卡 0 不通，后面全是空中楼阁。

| 关卡 | 阶段名称 | 建议周期 | 核心目标 |
| --- | --- | --- | --- |
| 关卡 0 | 上线前急救 | 24-48 小时 | 把 5 项 Critical + 3 项配套高危一次性清零，让站点敢上公网 |
| 关卡 1 | 会话与 CSRF 加固 | Week 1 | refresh 轮换、CSRF、账户级限流、敏感依赖升级、admin 审计 |
| 关卡 2 | 性能引擎升级 | Week 2 | DB 连接池 + 列表缓存 + 图片优化 + 压缩，把首屏和 p95 拉回正常水位 |
| 关卡 3 | 体验与缓存收敛 | Week 3-4 | fetch 并行、bundle 瘦身、revalidate 风暴收敛、索引补齐 |
| 关卡 4 | 长期债务偿还 | Month 2 | 分页架构统一、XSS 转义、CSS/字典拆分、HMAC pepper |
| 关卡 5 | 长期安全/性能治理 | 持续 | 监控告警、依赖巡检、容量演练、SLO/SLA、年度渗透测试 |

总投入估算：**1 个后端 + 1 个前端 + 0.5 个 DevOps，约 6 人周**完成关卡 0-3；关卡 4-5 随业务迭代摊销。

---

## 3. 分关卡说明

### 关卡 0：上线前急救（24-48 小时）

**目标**

把"任意一项被外部利用就可以让站点凉一夜"的 Critical 项一次性清零。这关没过，**别上公网**，怎么强调都不过分。

**核心任务**

- **C0.1 密钥治理**
  - 立刻 `git rm --cached .env && git commit`，把 `.env` 加进 `.gitignore`
  - 视密钥为**已泄漏**：轮换 `DATABASE_URL` 密码、`JWT_SECRET`、`JWT_REFRESH_SECRET`、`R2_*` 全部凭据
  - 删除 [apps/api/src/index.ts:168-170](apps/api/src/index.ts#L168) 的 JWT fallback：`process.env.JWT_SECRET ?? "replace_me_..."` 改为启动时 `if (!secret || secret.length < 32) throw new Error(...)`
  - 用 `crypto.randomBytes(64).toString('hex')` 重新生成两套密钥写入生产环境变量管理
- **C0.2 Cookie Secure 全员对齐**
  - 抽公共常量 `COOKIE_DEFAULTS`，`secure: process.env.NODE_ENV === "production"`
  - 替换 7 处硬编码：[login/route.ts](apps/web/app/api/auth/login/route.ts) × 3、[session/route.ts](apps/web/app/api/auth/session/route.ts) × 2、[logout/route.ts](apps/web/app/api/auth/logout/route.ts)、[middleware.ts](apps/web/middleware.ts)、[apps/api/src/index.ts:771](apps/api/src/index.ts#L771)
- **C0.3 滑块验证码替换**
  - 评估方案：Cloudflare Turnstile（免费、隐私友好）vs hCaptcha
  - 后端 `/v1/auth/login` 必须校验 `cf-turnstile-response` token，校验失败直接 401
  - 删除 [slider-captcha.tsx](apps/web/components/slider-captcha.tsx) 或保留 UI 但内部接 Turnstile widget
- **C0.4 RateLimit 逻辑修复**
  - 修复 [apps/api/src/index.ts:222-248](apps/api/src/index.ts#L222) 的 `if (!redisClient) { next(); return; }` —— 这一行导致 Redis 没起时所有限流被旁路
  - 短期：去掉这一行，让内存桶兜底
  - 中期：换 `rate-limiter-flexible`，自动 fallback Redis → Memory
- **C0.5 pg.Pool 替换 pg.Client**
  - [apps/api/src/index.ts:179](apps/api/src/index.ts#L179) `new Client` 改 `new Pool({ max: 10, idleTimeoutMillis: 30000 })`
  - 全文 `pgClient.query` → `pool.query`（IDE 全局替换）
  - 事务（如有）改用 `const client = await pool.connect()` + `try/finally` 释放
- **C0.6 错误堆栈不外漏**
  - [apps/api/src/index.ts:1953](apps/api/src/index.ts#L1953) 全局 error handler：生产模式仅返回 `{ code: "INTERNAL", message: "Internal server error", reqId }`
  - 详细 stack 写 `console.error` + reqId（结构化）
  - 同样处理散布在 [index.ts:866,898,929-967](apps/api/src/index.ts#L866) 等多处直接回 `error.message` 的位置
- **C0.7 调试日志清理**
  - 删除 [apps/web/app/api/prompts/route.ts:70-216](apps/web/app/api/prompts/route.ts#L70) 全部 `console.log`（含 `rawBody.slice(0,500)`、`payloadSummary`、token cookie 状态等）
  - 全仓 grep `console.log` 评估清理范围

**交付物**

- 一份"密钥轮换记录"，列出哪几个密钥换了、新值放在哪个 secret store
- 一个 `cookie-defaults.ts` 公共模块
- 接入 Turnstile / hCaptcha 的 PR
- pg.Pool 改造 PR
- error handler 重构 PR
- 删除调试日志的 PR

**验收标准**

- `git log --all -- .env` 验证 `.env` 不在历史版本中（如已推送，需 `git filter-repo` 重写历史并强推 + 通知协作者）
- 启动时不设 `JWT_SECRET` 环境变量服务必须 fail-fast
- 生产域名 cookie 在浏览器 DevTools 中 `Secure` 列为 ✓
- Postman 直接 POST `/v1/auth/login` 不带 captcha token 必须 401
- 制造一个 `throw new Error("test")` 让 client 收到 `"Internal server error"` 而不是堆栈
- `pg_stat_activity` 看 API 进程数 > 1 个连接

**风险提醒**

- **不要在生产数据库轮换密码后忘了同步密钥到 K8s Secret / .env.production**，会让服务起不来。建议先用蓝绿部署或滚动更新
- `git filter-repo` 是历史重写，**所有 fork / clone 都得重 clone**，提前公告
- 滑块验证码切 Turnstile 时记得加灰度（先 5% 流量），免得 captcha 服务挂掉打挂登录
- pg.Pool 改造后，**事务**必须用同一个 `client` 不能用 `pool`，否则两条 SQL 不在一个事务里 —— 重点 review 用了 `BEGIN/COMMIT` 的代码

---

### 关卡 1：会话与 CSRF 加固（Week 1）

**目标**

补齐"账户被盗 / 重放 / 暴力破解 / 跨站伪造"四大类 High 风险。这关做完，"用户系统"才算正经能用。

**核心任务**

- **C1.1 Refresh Token 轮换 + Reuse Detection**
  - [apps/api/src/index.ts:787-843](apps/api/src/index.ts#L787) `/v1/auth/refresh` 改造：每次刷新签发新 refresh，旧 session 标 `revoked_at = NOW()`
  - 检测到已撤销 refresh 被重放：撤销该用户所有 session（视为失窃）+ 安全告警
  - 数据库 `auth_sessions` 已有 `revoked_at` 字段，可直接用
- **C1.2 CSRF 防护**
  - 短期方案：所有 state-changing 接口（POST/PUT/DELETE）校验 `Origin` header 在白名单内（基于 `WEB_ORIGIN`）
  - 中期方案：引入 double-submit cookie token（如 `csrf_token` 非 httpOnly cookie + 表单/header 同步携带，后端比对一致性）
  - 范围：login/register/logout/publish/me/interactions/admin/moderation/uploads
- **C1.3 账户级失败锁定**
  - 不再仅按 IP 限流，按 `account` 累计失败次数
  - 5 次失败 → 锁 15 分钟；10 次失败 → 锁 1 小时；24 次失败 → 锁 24 小时
  - 用 Redis incr + ttl 实现，key 形如 `auth:fail:${account}`
- **C1.4 危险依赖升级**
  - `multer 1.x` → `multer 2.x`，或确认是死代码后从 `apps/api/package.json` 删除
  - `bcryptjs 2.x` → `argon2 ^0.31`，参数 `{ type: argon2.argon2id, memoryCost: 19456, timeCost: 2, parallelism: 1 }`（OWASP 推荐）
  - 老用户的 bcrypt 哈希通过"登录时静默升级"策略迁移：登录成功且当前哈希是 bcrypt 时，重新用 argon2 哈希入库
  - `next 16` + `react 18` 错配评估：升级 react 到 19
- **C1.5 Admin 视角接口拆分 + 审计**
  - 把 [apps/api/src/index.ts:1121-1178](apps/api/src/index.ts#L1121) `/v1/prompts/me` 中 admin 走"全量列表"的分支抽出为 `/v1/admin/prompts`
  - 新建 `audit_logs` 表：`id, actor_id, action, target_type, target_id, payload, ip, user_agent, created_at`
  - admin 接口写入 audit log，前端开"审计回放"页（关卡 5 做）
  - admin 接口加专属限流（10 req/min/admin）
- **C1.6 user_nickname Cookie 治理**
  - 删除 cookie + SSR 改通过 `/api/auth/session` 拉昵称（前面已经做过 middleware 自动 refresh，这里继续收敛）
  - 或保留但限定为 `^[A-Za-z0-9_-]{1,32}$`，明确仅作 UI hint
  - 别忘了 [user-nav.tsx](apps/web/components/user-nav.tsx) 同步逻辑

**交付物**

- refresh rotation + reuse detection PR
- CSRF 中间件 / Origin 校验 PR
- 账户级限流 PR
- argon2 迁移 + 静默升级 PR
- `/v1/admin/prompts` + audit_logs 迁移 PR
- 删除 / 收敛 user_nickname cookie 的 PR

**验收标准**

- 同一 refresh token 用两次必须第二次失败 + 该用户所有 session 被吊销
- curl POST `/v1/auth/login` 带伪造 `Origin: https://evil.com` 必须 403
- 同账号连续输错密码 6 次必须返回"账户已锁定 15 分钟"
- 老用户用旧密码登录后，DB 里该用户哈希前缀从 `$2b$` 变成 `$argon2id$`
- admin 操作产生 audit log 且不可被 admin 自己删除（仅 super_admin 可删）
- 浏览器 cookie 列表里要么 `user_nickname` 不存在，要么内容仅 ASCII 且未被 SSR 用于权限判断

**风险提醒**

- refresh rotation 实现错了会导致**老用户全部被踢下线**，先在 staging 跑 24 小时压测
- argon2 native binary 在不同 CPU 架构（M1 vs x86）需要预编译，CI 缓存别忘了
- CSRF Origin 校验可能误伤合法的 webhook 调用，先白名单审计 30 天再开严格模式

---

### 关卡 2：性能引擎升级（Week 2）

**目标**

把首屏 TTFB、LCP、p95 latency 三个核心性能指标拉到正常水位。这关的几项做完，用户能"明显感觉到快"。

**核心任务**

- **C2.1 Shell 提升到 layout**
  - 新建 `apps/web/app/(shell)/layout.tsx`，把 [shell.tsx](apps/web/components/shell.tsx) 的 header + footer 移过去
  - 12 个 page.tsx 全部去掉 `<Shell>` 包裹（home、search、models、publish、login、register、me 系列、prompts 详情、admin 审核）
  - 创建客户端组件 `<NavLinks>`，用 `usePathname()` 算 active state
  - 路由切换不再触发 UserNav unmount + cookies() 重新解析
- **C2.2 UserNav 仅 cookie 缺失时 fetch**
  - [user-nav.tsx:50](apps/web/components/user-nav.tsx#L50) `useEffect` 改为 `if (initialNickname) return` 早退
  - 或抽到 module-level Promise singleton，全站只发 1 次
- **C2.3 Prompts 列表 Redis 缓存 + facet 合并**
  - [apps/api/src/index.ts:971-1119](apps/api/src/index.ts#L971) `/v1/prompts` 列表（无 token 路径）30s Redis 缓存
  - key 形如 `prompts:list:${md5(JSON.stringify(query))}`
  - 4 个 facet SQL 合并成单条 `GROUPING SETS` 一次往返
  - 移除 [apps/api/src/index.ts:1104](apps/api/src/index.ts#L1104) `getModelLabel` 对 SQL `model_label` 字段的覆盖
- **C2.4 PromptCard / Gallery 换 next/image**
  - [prompt-card.tsx:78-90](apps/web/components/prompt-card.tsx#L78) 和 [prompt-gallery.tsx:30-54](apps/web/components/prompt-gallery.tsx#L30) 全部 `<img>` 换 `<Image>`
  - 加 `sizes="(max-width:720px) 50vw, 320px"`、`priority={index<2}`
  - [next.config.mjs](apps/web/next.config.mjs) 补 `formats: ['image/avif','image/webp']`、`minimumCacheTTL: 30*24*60*60`、`dangerouslyAllowSVG: false`
- **C2.5 API 网络中间件**
  - [apps/api/src/index.ts:166-200](apps/api/src/index.ts#L166) 加：
    - `app.use(compression({ threshold: 1024 }))` —— 列表 JSON 减 60-70%
    - `app.set('trust proxy', 1)` —— 让 `req.ip` 走 X-Forwarded-For
    - `app.use(helmet({ contentSecurityPolicy: false }))` —— 加基本安全头
- **C2.6 fallback 图片本地化**
  - [apps/web/lib/data.ts:151-263](apps/web/lib/data.ts#L151) 所有 `images.unsplash.com` 链接迁到 R2 公共桶
  - 写脚本：`pnpm tsx scripts/migrate-fallback-images.ts`，下载 + 上传 + 替换 URL

**交付物**

- 新 `(shell)/layout.tsx` + `<NavLinks>` 客户端组件 + 12 个 page.tsx 改造
- UserNav 优化 PR
- Prompts 列表 Redis 缓存 + facet SQL 合并 PR
- next/image 迁移 PR + next.config 优化
- compression / trust proxy / helmet 三件套 PR
- fallback 图迁 R2 脚本 + 数据更新

**验收标准**

- Lighthouse 测首页：LCP < 2.5s（mobile 4G simulated）
- Chrome DevTools Network：Prompts 列表 JSON 响应 `Content-Encoding: gzip`，体积 ↓ 60%+
- DataDog / Grafana：API p95 latency 较关卡 2 前 ↓ 50%
- 路由切换从 `/` 到 `/search`：UserNav 不重新挂载（React DevTools profile 验证）
- `/v1/prompts` 列表第二次请求命中 Redis（`X-Cache: HIT` header 或日志）
- `req.ip` 在 Cloudflare 后能拿到真实 IP

**风险提醒**

- Shell 提升到 layout 时，注意 `dynamic` 和 `revalidate` 配置不要丢；`(shell)` 路由组本身不影响 URL
- next/image 改造时，对**外站图片**记得在 `remotePatterns` 加白名单，不然渲染白屏
- helmet 默认开 CSP 会挡很多 inline script / unsafe-eval，先 `contentSecurityPolicy: false` 关掉，关卡 4 再补 CSP
- Redis 缓存 key 一定要包含 query 参数哈希，否则不同筛选条件会串数据
- 切 next/image 后，开发环境不要忘了启动 `next/image` 的本地优化服务（默认开），不然会 404

---

### 关卡 3：体验与缓存收敛（Week 3-4）

**目标**

打掉那些"不影响功能但用户能感觉到卡"的地方，并且把缓存失效逻辑收敛，避免互动操作引发 ISR 雪崩。

**核心任务**

- **C3.1 SSR fetch 并行重排**
  - [page.tsx:14-30](apps/web/app/page.tsx#L14) home：`fetchCurrentUser` 并入 Promise.all
  - [prompts/[id]/page.tsx:70-82](apps/web/app/prompts/[id]/page.tsx#L70) detail：4 个 fetch 一次性并行（id 已从 params 拿到）
  - `getDictionary()` 用 `React.cache` 包装
- **C3.2 重型 Client 组件 dynamic import**
  - [publish-loading-overlay.tsx](apps/web/components/publish-loading-overlay.tsx) 在 publish 页 `next/dynamic({ ssr:false, loading: () => null })`
  - [slider-captcha.tsx](apps/web/components/slider-captcha.tsx) 同样 dynamic（如果关卡 0 没换 Turnstile 还在用）
  - [typewriter-headline.tsx](apps/web/components/typewriter-headline.tsx) dynamic + `requestIdleCallback`
- **C3.3 revalidateTag 风暴收敛**
  - [me/interactions/route.ts:43-46](apps/web/app/api/me/interactions/route.ts#L43) 仅 invalidate 详情 path/tag，不动列表
  - 列表更新依靠已有的 BroadcastChannel cache-sync 触发当前 tab `router.refresh`
- **C3.4 索引补齐**
  - [packages/database/src/schema.sql](packages/database/src/schema.sql) 加部分索引：
    ```sql
    CREATE INDEX CONCURRENTLY idx_prompts_copy_count ON prompts (copy_count DESC) WHERE status='approved';
    CREATE INDEX CONCURRENTLY idx_prompts_like_count ON prompts (like_count DESC) WHERE status='approved';
    CREATE INDEX CONCURRENTLY idx_prompts_collect_count ON prompts (collect_count DESC) WHERE status='approved';
    CREATE INDEX CONCURRENTLY idx_prompts_usage_tags_gin ON prompts USING GIN (usage_tags);
    CREATE INDEX CONCURRENTLY idx_prompts_color_tags_gin ON prompts USING GIN (color_tags);
    ```
  - 写迁移文件，**生产用 `CONCURRENTLY` 避免锁表**
- **C3.5 Dictionaries 拆 namespace**
  - 把 [dictionaries.ts](apps/web/lib/i18n/dictionaries.ts) 1248 行拆成按 namespace（home/login/register/search/prompts/...）
  - server 组件按需 import，client 组件通过 props 接收切片
  - 重点：[prompt-card.tsx:30](apps/web/components/prompt-card.tsx#L30) 不再 `import { getDictionary }`
- **C3.6 TypewriterHeadline 优化**
  - [typewriter-headline.tsx:69-72](apps/web/components/typewriter-headline.tsx#L69) `MutationObserver(document.body, subtree:true)` 换 `IntersectionObserver` 或 `useLayoutEffect` 跑一次
  - 或监听 `next/navigation` 的 `usePathname` 变化精准触发
- **C3.7 列表分页改真分页**
  - [me/prompts/page.tsx:38-40](apps/web/app/me/prompts/page.tsx#L38) `allRecords.slice()` 客户端分页改 API 接收 `page` & `pageSize`
  - 后端 [/v1/prompts/me](apps/api/src/index.ts#L1121) 加 `LIMIT $offset, $size` 或 cursor 分页
  - me/collections、admin/moderation 同样处理

**交付物**

- 4 个 SSR 页面的 fetch 重排 PR
- 3 个重型组件 dynamic import PR
- revalidate 收敛 PR
- 5 条索引迁移文件 + 灰度上线
- dictionaries 拆分（含 codemod 脚本）
- TypewriterHeadline 重写
- 真分页 API + 前端 PR

**验收标准**

- Chrome Coverage 工具：First Load JS 较关卡 3 前 ↓ 20%+
- 详情页 TTFB ↓ 100-200ms（Lighthouse 对比）
- 高频点赞场景：单次 like 操作不再触发全站列表 ISR 失效（Next.js cache hit metric 验证）
- `EXPLAIN ANALYZE` trending 排序查询命中新索引（Index Scan 而非 Seq Scan）
- Bundle Analyzer：客户端不再包含 dictionaries 全量 locale
- 路由切换时 TypewriterHeadline 触发次数 = 1 而非数十次

**风险提醒**

- `CREATE INDEX CONCURRENTLY` 不能在事务里跑，迁移工具要支持
- dictionaries 拆分会改大量 import 路径，建议写 codemod 一次替换，别手动改
- 真分页改造后，前端的"页码 URL"格式如果变，要 301 重定向旧 URL，否则丢 SEO
- dynamic import 会产生额外 chunk request，第三世界用户网络差时反而慢，**重点组件 `prefetch: true`**

---

### 关卡 4：长期债务偿还（Month 2）

**目标**

清理那些"做了不会立刻见效但不做以后必出事"的中低危项。

**核心任务**

- **C4.1 密码强度增强**
  - 后端 [apps/api/src/index.ts:622](apps/api/src/index.ts#L622) 校验：长度 ≥ 10 + 含字母 + 含数字 + 黑名单（用 `zxcvbn` 库或自建 1000 常用密码字典）
  - 前后端规则对齐，前端用 `zxcvbn` 实时给强度反馈
- **C4.2 XSS 死角清理**
  - [prompts/[id]/page.tsx:126](apps/web/app/prompts/[id]/page.tsx#L126) ld+json：`JSON.stringify(data).replace(/</g, "\\u003c")`
  - [prompt-card.tsx:80](apps/web/components/prompt-card.tsx#L80)、[prompt-gallery.tsx:32,50](apps/web/components/prompt-gallery.tsx#L32) `<img src>` URL 协议白名单（http / https / 站内相对路径）
  - 后端 prompt 创建接口 [apps/api/src/index.ts:1297-1307](apps/api/src/index.ts#L1297) 同样校验 `image.url`
  - moderation 视图打 `data-trusted` 标记
- **C4.3 输入校验补齐**
  - 引入 zod 统一 API 参数校验
  - telemetry payload 显式 `if (JSON.stringify(payload).length > 8192) return 400`
  - 上传 filename 加 `\0` 字节过滤
- **C4.4 CORS 收紧**
  - [apps/api/src/index.ts:190-198](apps/api/src/index.ts#L190) 显式声明 `methods: ["GET","POST","PUT","DELETE","PATCH"]`、`allowedHeaders: ["Content-Type","Authorization"]`
  - 启动时校验 `WEB_ORIGIN` 不为空且包含 `https://`，否则 fail-fast
- **C4.5 cover_url 物化**
  - `prompts` 表加 `cover_url TEXT` 列
  - 创建 trigger：`prompt_images` insert/delete 时同步更新 `prompts.cover_url = (SELECT url FROM prompt_images WHERE prompt_id=NEW.prompt_id ORDER BY sort_order LIMIT 1)`
  - 修改 [/v1/prompts](apps/api/src/index.ts#L1019) 列表 SQL 直接 SELECT `prompts.cover_url`，去掉子查询
- **C4.6 globals.css 拆模块**
  - 把 [globals.css](apps/web/app/globals.css) 1929 行拆为：
    - `globals.css` 仅留变量 + reset + 排版基础（< 300 行）
    - `publish-loading.module.css`
    - `captcha.module.css`
    - `scrollbar.css`（lazy load）
    - `typewriter.module.css`
    - `prompt-card.module.css`
  - `will-change` 在 animationend 用 JS 移除
- **C4.7 refresh hash 升级**
  - [apps/api/src/index.ts:332](apps/api/src/index.ts#L332) `hashToken` 从 `sha256` 改 HMAC-SHA256：`crypto.createHmac('sha256', process.env.REFRESH_TOKEN_PEPPER!).update(token).digest('hex')`
  - 老 hash 通过下次 refresh 时自然轮换
- **C4.8 错误码标准化**
  - 后端定义错误码常量：`AUTH_TOKEN_EXPIRED`、`AUTH_TOKEN_INVALID`、`AUTH_TOKEN_REVOKED`、`AUTH_NEED_LOGIN`
  - 前端拿到 `AUTH_TOKEN_EXPIRED` 触发 silent refresh，其他直接踢登录页

**交付物**

- 密码强度增强 PR + 黑名单字典文件
- XSS 三处转义 PR
- zod 统一校验骨架 + 5 个核心接口接入示范
- CORS 收紧 PR
- cover_url 物化迁移 + trigger
- CSS 拆模块 + 文件清单
- HMAC pepper 迁移 + 环境变量补齐
- 错误码常量与前端处理 PR

**验收标准**

- 注册"password123" 必须返回密码强度不够
- ld+json 内容包含 `</script>` 时输出转义后的 `<`
- `image_url=javascript:alert(1)` 提交必须 400
- API 调 OPTIONS 预检请求返回的 `Access-Control-Allow-Methods` 与白名单一致
- prompts 列表 EXPLAIN 结果不再有 `SubPlan`
- Lighthouse Coverage：首屏 CSS 较关卡 4 前 ↓ 50%
- 旧 refresh token 在第 2 次刷新后已变成 HMAC hash
- 前端拿 `AUTH_TOKEN_EXPIRED` 自动 silent refresh，不弹登录框

**风险提醒**

- 密码强度增强后**注册成功率会下降 5-10%**，运营做好心理准备
- ld+json 转义如果做错会让 Google 搜索结构化数据校验失败，上线前用 [Rich Results Test](https://search.google.com/test/rich-results) 验证
- cover_url 物化的 trigger 性能要测，发图密集时 trigger 会成为瓶颈；可用 `LISTEN/NOTIFY` 异步刷
- HMAC pepper 写入环境变量后，**该变量丢了 = 所有 refresh token 失效 = 全员强制重新登录**，做好 vault 备份

---

### 关卡 5：长期安全/性能治理（持续）

**目标**

把"治理"从"打补丁"变成"自动化巡检 + 演练"，让之前关卡的成果能持续保鲜。

**核心任务**

- **C5.1 监控告警**
  - 接入 Sentry / Datadog / Grafana 三选一
  - 关键告警：
    - 5xx 响应率 > 1% 持续 5 分钟
    - p95 latency > 1s 持续 10 分钟
    - DB 连接池使用率 > 80%
    - Redis 命中率 < 70%
    - 单账号失败登录 > 50 次/小时
- **C5.2 依赖巡检**
  - 接入 Dependabot / Renovate，每周自动 PR 升级
  - GitHub Actions 加 `pnpm audit` + `npm-check-updates` 周度任务
  - 高危 CVE 24 小时内必须有人响应（值班轮换）
- **C5.3 Audit Log 仪表板**
  - 关卡 1 已建 `audit_logs` 表，这关做查询面板
  - admin 页面：按 actor / target / action / 时间范围筛
  - 高敏操作（删 prompt / 改用户角色 / 大批量审核）发飞书 / 钉钉机器人
- **C5.4 容量演练**
  - 每季度 1 次：用 k6 / wrk 模拟 10× 当前流量，看哪个组件先倒
  - 演练后写"瓶颈复盘"，标进下一关卡的优化清单
- **C5.5 SLO/SLA 定义**
  - 内部 SLO：API 可用性 99.5%、p95 < 500ms、prompts 列表 cache 命中率 > 80%
  - 对用户 SLA：暂不承诺（社区产品），但内部按 SLO 跟进
- **C5.6 年度渗透测试**
  - 上线 6 个月内做一次第三方渗透测试（白盒）
  - 重点：refresh rotation、CSRF、CORS、文件上传 SSRF、admin 越权
- **C5.7 数据保留与脱敏**
  - 请求日志保留 30 天，IP/UA 字段 90 天后聚合脱敏
  - 用户删除账号时，关联 prompt 改 author 为 `__deleted__`、interactions 软删除

**交付物**

- 监控仪表板 + 告警规则
- Dependabot/Renovate 配置 + 值班 SOP
- audit log 查询页面
- 季度容量演练报告
- SLO 文档 + 月度回顾会
- 第三方渗透测试报告 + 修复 PR
- 数据保留策略文档 + 自动清理 cron

**验收标准**

- 在 staging 制造 5xx 风暴 5 分钟内告警通道收到通知
- Dependabot 每周至少产出 1 个升级 PR
- audit log 页面可按"过去 7 天 admin 删除操作"过滤
- 季度演练报告归档于 `docs/capacity-test/YYYY-Qn.md`
- SLO 月度回顾会议纪要存档
- 渗透测试 High/Critical 项 30 天内全部修复

**风险提醒**

- 监控告警配多了会被"狼来了"麻木，先少配几个真正影响业务的，渐进加
- Dependabot 自动 PR 看起来省心，**绝对不要开 auto-merge**，必须 CI 全绿 + 人工 review
- 渗透测试找的厂商签 NDA，避免泄漏内部信息
- 数据保留与脱敏涉及 GDPR / 个保法合规，建议同时让法务过一遍

---

## 4. 整体节奏图

```
Day 0          Day 2         Week 1        Week 2        Week 4        Month 2       持续
  |              |              |             |             |             |            |
关卡 0  ──→  公网可上    ──→  关卡 1  ──→  关卡 2  ──→  关卡 3  ──→  关卡 4  ──→  关卡 5
急救                       会话/CSRF      性能引擎    体验/缓存    长期债务      治理巡检
(48h)                       (Week 1)      (Week 2)    (W3-4)       (Month 2)
```

- **关卡 0** 不通 → 一律不准上公网
- **关卡 0-2** 是连续 sprint，建议同一批人闭环干完
- **关卡 3-4** 可与正常业务迭代并行
- **关卡 5** 是长期心法，融入团队节奏即可

---

## 5. 责任分工建议

| 模块 | 关卡 0 | 关卡 1 | 关卡 2 | 关卡 3 | 关卡 4 | 关卡 5 |
| --- | --- | --- | --- | --- | --- | --- |
| 后端 | C0.1, C0.4, C0.5, C0.6, C0.7 | C1.1, C1.3, C1.4, C1.5 | C2.3, C2.5 | C3.4, C3.7 | C4.1, C4.3, C4.4, C4.5, C4.7, C4.8 | C5.1, C5.3, C5.4 |
| 前端 | C0.2, C0.3 | C1.2, C1.6 | C2.1, C2.2, C2.4, C2.6 | C3.1, C3.2, C3.3, C3.5, C3.6 | C4.2, C4.6 | C5.3 |
| DevOps | C0.1（密钥轮换、git 历史） | C1.4（CI 缓存、native binary） | C2.5（compression / helmet） | C3.4（迁移工具） | — | C5.1, C5.2, C5.6, C5.7 |

---

## 6. 总结

这份开发周期把"安全性能.md"那 50+ 项发现拆成了 6 个 sprint。**别把 P0 想成"我以后会做"——你今天不做，明天 GitHub 历史里的 `.env` 就被人扫到了**。

关卡 0 的 8 项任务是真正的"上线前底线"。剩下的 5 关按节奏做，可以让 Deeprompt 在三个月内从"功能能跑"进入"可以放心上公网且能扛流量"的状态。

不要在关卡 0 没做完就跳到关卡 2 优化性能 —— 一个被 JWT 伪造攻破的服务再快也只是死得更快。
