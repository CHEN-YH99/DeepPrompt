# Deeprompt 工程启动说明（关卡 0）

## 1. 环境要求

- Node.js 20+
- npm 10+

## 2. 首次安装

在仓库根目录执行：

```bash
npm install
```

## 3. 环境变量

复制根目录 `.env.example` 为 `.env` 并按本地环境修改：

```bash
copy .env.example .env
```

其中后端端口变量使用 `API_PORT`，默认是 `3010`；前端开发服务固定使用 `3000`，别让它和 API 抢端口。

## 4. 启动开发环境

这个仓库是 Monorepo，前后端分开跑：

终端 1（前端）：

```bash
npm run dev:web
```

终端 2（后端）：

```bash
npm run dev:api
```

默认地址：

- Web: http://localhost:3000
- API Health: http://localhost:3010/health

## 4.1 关卡 1：本地基础设施（PostgreSQL + Redis）

先启动数据库和缓存：

```bash
docker compose up -d
```

然后执行数据库初始化（迁移）：

```bash
npm run db:init
```

## 5. 常用命令（根目录）

```bash
npm run lint
npm run typecheck
npm run build
```

## 6. 当前关卡 0 交付结果

- `apps/web/app` 源码目录已存在，可独立开发，不依赖 `.next` 残留产物
- `apps/api/src` 已补齐并可启动基础服务
- 根目录已补齐 `package.json`、`turbo.json`、`tsconfig.base.json`、ESLint / Prettier / Husky 配置
- 已提供 `.env.example` 与本文档，支持新同学快速起项目
- 已补齐 `packages/database`、`packages/types`、`packages/ui`、`packages/config`
- 已提供 `docker-compose.yml`，支持本地一键拉起 PostgreSQL + Redis
- 已打通基础认证接口：`/v1/auth/register`、`/v1/auth/login`、`/v1/auth/refresh`、`/v1/auth/me`
