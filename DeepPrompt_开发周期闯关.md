# DeepPrompt 开发周期闯关

## 1. 文档定位

这不是那种拿来开会装忙的空心排期表，而是一份基于现有 PRD、TDD 和仓库真实状态整理出来的开发闯关文档。

目标是把 DeepPrompt 从“文档很完整、源码很飘忽”的状态，推进到可内测、可公测、可持续迭代的产品状态。

## 2. 编制依据

### 2.1 需求依据

- `DeepPrompt_需求文档.md`
- 核心范围以 MVP（P0）为主，覆盖：
- Prompt 浏览 / 搜索 / 筛选
- Prompt 详情页
- 用户注册 / 登录
- Prompt 发布 / 审核
- 收藏 / 点赞 / 复制
- 3 大模型分区（Model Registry 驱动）

### 2.2 技术依据

- `DeepPrompt_技术实现文档.md`
- 目标技术栈：
- 前端：Next.js 14 App Router
- 后端：NestJS + Fastify
- 数据库：PostgreSQL 16
- 缓存 / 队列：Redis 7 + BullMQ
- 搜索：Meilisearch
- 存储：Cloudflare R2

### 2.3 当前仓库现实

截至 2026-05-04，仓库内已确认的情况如下：

- PRD、TDD 两份主文档完整存在
- `apps/web/.next` 构建产物存在，说明前端页面曾经被启动和编译过
- `apps/web/dev.stdout.log` 显示历史上已编译过 `/`、`/search`、`/publish`、`/me/prompts`、`/login`、`/prompts/[id]`
- 但当前仓库缺少 `apps/web/app` 等关键源码目录
- `apps/web/dev.stderr.log` 明确报错：`ENOENT: no such file or directory, scandir 'E:\个人项目\DeepPrompt\apps\web\app'`

结论很直接：

- 当前不能把项目视为“已进入功能开发中后段”
- 当前更像“文档完成 + 有历史运行痕迹 + 源码未完整入库”的状态
- 所以开发周期必须从“工程归档修复”开始，而不是直接假装进入 Week 3

## 3. 开发总周期建议

建议分为两段：

- MVP 闯关周期：12 周
- V1.1 扩展周期：8 周

其中 MVP 周期保留 TDD 里的 12 周目标，但在执行顺序上增加一个 `关卡 0`，专门处理源码缺失、工程结构不完整、环境不可复现的问题。

## 4. 闯关总览

| 关卡 | 阶段名称 | 建议周期 | 核心目标 |
| --- | --- | --- | --- |
| 关卡 0 | 工程归档修复 | Week 1 | 让仓库从“能看文档”变成“能稳定开发” |
| 关卡 1 | 基础骨架搭建 | Week 2-3 | 完成 Monorepo、数据库、认证、基础前后端骨架 |
| 关卡 2 | Prompt 主链路打通 | Week 4-5 | 完成 Prompt 浏览、详情、发布、图片上传主流程 |
| 关卡 3 | 搜索与模型驱动 | Week 6-7 | 完成搜索筛选、Model Registry、动态参数表单 |
| 关卡 4 | 审核与互动闭环 | Week 8-9 | 完成审核队列、点赞收藏复制、个人中心基础能力 |
| 关卡 5 | 内测冲刺 | Week 10-11 | 完成性能优化、内容准备、Bug 收敛、种子用户内测 |
| 关卡 6 | 公测上线 | Week 12 | 完成上线、监控、SEO、运营启动 |
| 关卡 7 | V1.1 增长扩展 | Month 4-5 | 完成评论、关注、积分成就、新模型接入 |

## 5. 分关卡说明

### 关卡 0：工程归档修复

**目标**

把项目从“历史构建产物还在，源码却没进仓”的离谱状态拉回正轨。说白了，这关不打通，后面所有排期都是纸上蹦迪。

**核心任务**

- 核对并补齐前端源码目录，至少恢复 `apps/web` 的真实源码结构
- 明确后端目录是否存在，若不存在则按 TDD 的 Monorepo 结构补建 `apps/api`
- 补齐根目录工程文件：
- `package.json`
- `turbo.json`
- `tsconfig` 体系
- ESLint / Prettier / Husky 基础配置
- 明确依赖安装、启动、构建、测试命令
- 建立 `.env.example`
- 输出一份《工程启动说明》

**交付物**

- 可运行的 Monorepo 工程骨架
- 前端源码不再依赖 `.next` 残留物
- 后端基础服务目录就位
- 本地启动说明文档

**验收标准**

- 新同学拉代码后，可在 30 分钟内跑起前端和后端
- `apps/web/app`、`apps/api/src` 等核心目录真实存在
- 不再出现依赖构建残留才能启动的情况

**风险提醒**

- 如果源码已经丢了，就别自欺欺人说“项目完成 30%”，那叫历史缓存还活着，不叫项目活着
- 这关最怕的是有人拿 `.next` 当源码遗迹考古，搞半天还是没法继续开发

---

### 关卡 1：基础骨架搭建

**目标**

按 TDD 的目标技术栈把基础设施立起来，为后续功能开发提供统一底座。

**核心任务**

- 建立 Monorepo 目录：
- `apps/web`
- `apps/api`
- `packages/database`
- `packages/types`
- `packages/ui`
- `packages/config`
- 设计并落地 PostgreSQL Schema：
- `model_registry`
- `users`
- `prompts`
- `prompt_images`
- `interactions`
- `comments`
- `collections`
- `point_logs`
- 建立迁移机制
- 接入 Redis
- 完成认证基础方案选型并落地：
- 邮箱登录
- OAuth 预留
- JWT / Session 策略
- 建立公共类型与 API 响应约定

**交付物**

- 可迁移数据库 schema
- 基础认证链路
- 前后端共享类型
- 基础 UI 组件和布局骨架

**验收标准**

- 数据库可一键初始化
- 用户可完成注册 / 登录 / 登录态校验
- 前后端本地联调可跑通

**风险提醒**

- 认证方案别一上来又 NextAuth、又 Clerk、又自研 JWT，选型摇摆会把节奏搞烂
- Schema 不要边写边改得像泥巴，尤其 `model_registry` 和 `prompts`，它们是系统主轴

---

### 关卡 2：Prompt 主链路打通

**目标**

完成用户最核心的“看 Prompt、发 Prompt、看详情”的业务闭环。

**核心任务**

- 首页 Prompt 列表
- Prompt 卡片组件
- 精选 / 最新基础分区
- 详情页
- 大图预览
- Prompt 正文展示
- Negative Prompt 展示
- 参数区展示
- 一键复制
- 发布页
- 分步表单
- 图片上传 1~6 张
- 模型选择
- 标签和说明填写
- 后端 Prompt CRUD API
- 图片元数据存储
- R2 上传链路

**交付物**

- 首页
- 详情页
- 发布页
- Prompt CRUD API
- 图片上传能力

**验收标准**

- 用户可以创建一条 Prompt 并在前台看到
- 每条 Prompt 至少可关联 1 个模型和 1 张图片
- 详情页支持复制 Prompt 内容

**风险提醒**

- 不要先沉迷做花里胡哨动效，主链路没通之前搞视觉秀肌肉，属于典型本末倒置
- 上传链路要先定好对象存储命名、缩略图策略、元数据结构，不然后面图片系统会烂得很快

---

### 关卡 3：搜索与模型驱动

**目标**

把 DeepPrompt 做出差异化，不只是一个卡片墙，而是一个能检索、能扩展模型、能配置参数的 Prompt 平台。

**核心任务**

- 接入 Meilisearch
- 建立 Prompt 搜索索引
- 支持关键词搜索
- 支持模型 / 风格 / 色调 / 用途筛选
- 支持最新 / 最热 / 收藏 / 复制排序
- 建立 `model_registry` 数据管理能力
- 前端模型专区
- 发布页动态参数表单
- 详情页模型信息渲染
- 实现模型配置驱动页面逻辑

**交付物**

- 搜索页
- 筛选器
- Model Registry 管理基础能力
- 动态参数表单

**验收标准**

- 新增一个模型配置后，可在前端筛选器和发布表单中自动出现
- 搜索接口 P95 响应接近 TDD 目标
- 搜索结果能按多维条件稳定返回

**风险提醒**

- `model_registry` 如果没设计好，后面每加一个模型都得改代码，那这个“扩展性设计”就成了笑话
- 搜索索引字段别偷懒，后面要补全会很痛

---

### 关卡 4：审核与互动闭环

**目标**

让平台具备社区最基本的治理与互动能力，避免变成“谁都能发、发完没人管”的野摊子。

**核心任务**

- Prompt 审核状态机：
- `draft`
- `pending`
- `approved`
- `rejected`
- `archived`
- BullMQ 审核队列
- 内容安全校验预留
- 点赞 / 收藏 / 复制接口
- 幂等与计数器更新
- 个人中心基础页：
- 我的 Prompt
- 草稿 / 待审核 / 已发布
- 收藏列表基础版

**交付物**

- 审核流转机制
- 互动接口
- 个人中心基础功能

**验收标准**

- 新发布 Prompt 默认进入待审核
- 审核通过后才出现在公开列表
- 点赞 / 收藏 / 复制计数正确且避免重复刷数

**风险提醒**

- 互动计数如果直接无脑写库，高并发下一定乱
- 审核流如果没有明确状态转换规则，运营和开发最后会互相骂娘

---

### 关卡 5：内测冲刺

**目标**

从“功能能跑”推进到“用户能用”，开始收敛 Bug、准备内容、做性能兜底。

**核心任务**

- 补齐基础埋点和错误监控
- 优化首屏加载、图片加载和列表性能
- 做 SEO 基础项：
- Metadata
- Sitemap
- 结构化数据
- 准备 500+ 初始 Prompt 内容
- 开启种子用户邀请
- 修复 P0 / P1 Bug

**交付物**

- 内测版本
- 缺陷清单与修复记录
- 首批内容库
- 基础运营素材

**验收标准**

- 首页、搜索页、详情页、发布页、个人中心都能稳定使用
- 核心路径无阻断型 Bug
- 有足够冷启动内容支撑内测体验

**风险提醒**

- 没内容就开社区，跟开饭店不备菜一个德行，用户进来一看三屏翻完，直接走人
- 内测期不要贪功能扩张，先把主流程 Bug 压下去

---

### 关卡 6：公测上线

**目标**

完成正式上线和基础可运维能力建设，别搞成“刚发出去就趴窝”。

**核心任务**

- 部署前端、后端、数据库、Redis、Meilisearch、对象存储
- 接入监控与告警：
- 前端性能
- API 错误率
- 数据库慢查询
- 队列堆积
- 应用日志聚合
- 上线前回归测试
- 发布冷启动活动

**交付物**

- 公测环境
- 监控与告警配置
- 上线检查表
- 运营启动方案

**验收标准**

- 线上可稳定访问
- 关键接口与页面均有监控
- 出现错误能被及时发现和定位

**风险提醒**

- 只部署不监控，属于把系统丢进海里再祈祷它会游泳
- 上线前必须有回归清单，别靠“我觉得没问题”

---

### 关卡 7：V1.1 增长扩展

**目标**

在 MVP 站稳后，补足社区增长与留存能力。

**核心任务**

- 评论系统
- 关注 / 粉丝系统
- 积分与成就系统
- 排行榜
- 收藏夹增强
- 新模型接入：
- Stable Diffusion 3.5
- DALL·E 3
- AI 标签推荐接入
- Prompt 合集 / 专题策展

**交付物**

- V1.1 功能包
- 新模型配置上线
- 成长体系与社区互动增强

**验收标准**

- 用户能围绕 Prompt 形成互动沉淀
- 新模型接入流程不需要改动核心业务代码
- AI 标签推荐可进入实际发布流程

**风险提醒**

- MVP 没稳就急着做积分勋章，那是给半成品贴金边
- 评论、关注、通知链路要一起想，不然功能看着多，体验像碎玻璃

## 6. 角色分工建议

| 角色 | 核心职责 |
| --- | --- |
| 产品 | PRD 细化、验收标准、优先级裁剪、内测反馈收敛 |
| UI/UX | 页面原型、组件规范、响应式与交互细节 |
| 前端 | Next.js 页面、状态管理、上传交互、SEO、性能优化 |
| 后端 | API、认证、审核流、搜索接入、队列与缓存 |
| 数据 / 平台 | PostgreSQL、Redis、Meilisearch、R2、部署与监控 |
| 测试 / 运营 | 用例回归、种子用户组织、初始内容准备、冷启动活动 |

## 7. 关键里程碑验收表

| 时间点 | 里程碑 | 是否允许进入下一阶段 |
| --- | --- | --- |
| Week 1 结束 | 工程可启动，源码完整入库 | 未完成则后续全部顺延 |
| Week 3 结束 | 认证、数据库、前后端骨架稳定 | 未完成则禁止进入大规模页面开发 |
| Week 5 结束 | Prompt 主链路打通 | 未完成则禁止内测准备 |
| Week 7 结束 | 搜索和模型驱动可用 | 未完成则平台差异化不足，不建议继续冲上线 |
| Week 9 结束 | 审核与互动闭环完成 | 未完成则上线风险极高 |
| Week 11 结束 | 内测反馈收敛，核心 Bug 清零 | 未完成则不应公测 |
| Week 12 结束 | 公测上线 | 进入 V1.1 迭代 |

## 8. 当前建议执行顺序

如果按当前仓库状态立刻开工，建议顺序如下：

1. 先过 `关卡 0`，找回或重建真实源码和工程骨架。
2. 再过 `关卡 1`，把数据库、认证、Monorepo 底座定稳。
3. 接着优先完成 `关卡 2` 和 `关卡 3`，先打通业务主链路和模型驱动能力。
4. `关卡 4` 之后再做内测与公测准备，不要倒着来。

## 9. 一句话判断项目是否在正轨

满足下面 4 条，项目才算真正在正轨上：

- 源码完整可复现，不依赖历史构建产物
- Prompt 主链路可跑通
- 搜索和模型驱动真正落地
- 审核、监控、上线机制不是摆设

如果这 4 条有 2 条都没做到，就别吹什么平台化和生态化，先把基本盘做扎实，少整那些飘的。

