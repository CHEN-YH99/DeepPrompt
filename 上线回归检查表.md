# Deeprompt 上线回归检查表

> 每次部署到生产环境前，按此清单逐项确认。全部 ✅ 后方可发布。

## 一、环境与配置

- [ ] `.env` 所有必填变量已设置（DATABASE_URL / REDIS_URL / JWT_SECRET / JWT_REFRESH_SECRET）
- [ ] JWT_SECRET / JWT_REFRESH_SECRET 为强随机字符串（≥ 32 字符），非示例值
- [ ] R2 凭据已配置（R2_ACCOUNT_ID / R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY / R2_BUCKET_NAME / R2_PUBLIC_URL）
- [ ] WEB_ORIGIN 设为生产域名（如 `https://deepprompt.ai`）
- [ ] NEXT_PUBLIC_SITE_URL 设为生产 URL
- [ ] INVITE_REQUIRED 设为 `true`（公测初期限制注册）
- [ ] CORS origin 仅含生产域名 + localhost（开发用）
- [ ] `npm run typecheck` 通过
- [ ] `npm run lint` 通过
- [ ] `npm run build` 前端 + 后端均成功

## 二、数据库

- [ ] PostgreSQL 16 运行正常
- [ ] `npm run db:init` 已执行，schema.sql + schema-gate5.sql 建表成功
- [ ] `npm run seed:bulk` 已执行，512 条种子数据入库
- [ ] 验证 `SELECT COUNT(*) FROM prompts WHERE status = 'approved'` 返回 ≥ 500
- [ ] 数据库连接池正常，无慢查询告警
- [ ] 定时备份已配置（每日全量 + 增量）

## 三、Redis

- [ ] Redis 7 运行正常
- [ ] API 日志无 `[api] redis unavailable` 报错
- [ ] 会话存储可写入/读取

## 四、Meilisearch

- [ ] Meilisearch 实例运行正常（端口 7700）
- [ ] `MEILISEARCH_MASTER_KEY` 已配置
- [ ] Prompt 索引已初始化（或预留手动全量同步入口）
- [ ] 搜索 API 可返回结果

## 五、API 健康检查

- [ ] `GET /health` 返回 200 + `checks.database = "ok"`
- [ ] `GET /ready` 返回 200
- [ ] `GET /v1/models` 返回模型列表 + Cache-Control 头
- [ ] `GET /v1/prompts?limit=5` 返回 Prompt 列表 + facets
- [ ] `POST /v1/auth/register` 注册新用户成功
- [ ] `POST /v1/auth/login` 登录成功，返回 access_token + refresh_token
- [ ] `GET /v1/auth/me` 携 token 返回当前用户
- [ ] `POST /v1/uploads/presign` 返回 R2 预签名 URL
- [ ] `POST /v1/telemetry` 返回 202
- [ ] 限流生效：连续快速请求后收到 429

## 六、前端页面

- [ ] 首页（/）正常渲染，Prompt 列表可见
- [ ] 搜索页（/search）筛选器 + 结果正常
- [ ] 详情页（/prompts/:id）Prompt 内容 + 互动 + 相关推荐
- [ ] 发布页（/publish）登录后可访问，表单可提交
- [ ] 个人中心（/me/prompts + /me/collections）登录后可访问
- [ ] 登录 / 注册页正常
- [ ] 404 页（访问不存在的路径）显示 not-found.tsx
- [ ] 无控制台 JS 报错

## 七、SEO

- [ ] `/sitemap.xml` 可访问，包含 Prompt + Model URL
- [ ] `/robots.txt` 可访问，规则正确
- [ ] 详情页 `<title>` 动态显示 Prompt 标题
- [ ] 详情页含 `application/ld+json` 结构化数据
- [ ] OG / Twitter meta 标签正确

## 八、安全

- [ ] HTTPS 全站强制（HSTS header）
- [ ] API 无敏感信息泄露（stack trace / 内部 IP 不返回给客户端）
- [ ] Cookie 设置 HttpOnly + Secure + SameSite
- [ ] 密码存储使用 bcrypt（cost ≥ 12）
- [ ] 文件上传仅允许 jpg/png/webp
- [ ] 无硬编码密钥或凭证

## 九、监控

- [ ] `/health` 端点被外部监控服务探测（Better Stack Uptime / UptimeRobot）
- [ ] API 结构化日志输出正常（JSON 格式）
- [ ] 前端 telemetry 事件可写入 telemetry_events 表
- [ ] 异常事件可被 `/v1/admin/telemetry/summary` 查询
- [ ] 部署平台（Railway / Vercel）指标面板可访问

## 十、冷启动运营

- [ ] 种子 Prompt ≥ 500 条已入库
- [ ] 至少 1 个管理员账号已创建
- [ ] 至少 3 个邀请码已生成，分发给种子用户
- [ ] 首页 Banner / 精选标记至少 5 条 Prompt
